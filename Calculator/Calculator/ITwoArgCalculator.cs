namespace Calculator
{
    /// <summary>
    /// It's interface for two arguments of calculate
    /// </summary>
    public interface ITwoArgCalculator
    {
        double Calculate(double first, double second);
    }
}