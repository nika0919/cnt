namespace Calculator
{
    /// <summary>
    /// It's interface for one argument of calculate
    /// </summary>
    public interface IOneArgCalculator
    {
        double Calculate(double first);

    }
}